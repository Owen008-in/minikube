apiVersion: v1
kind: Service
metadata:
  name: subsonic-ha-svc
  namespace: subsonic
spec:
  type: NodePort
  selector:
    app: subsonic-ha
  ports:
    - name: http
      port: 80
      targetPort: 4040
